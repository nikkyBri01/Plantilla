let nombre = 'Nicole'


function sumar(x, y) {
    return x + y
}

const restar = (x, y) => {
    return x - y
}

const multiplicar = (x, y) => {
    return x * y
}

export default {
    nombre,
    restar,
    multiplicar,
    sumar
}